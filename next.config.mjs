/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['upload.wikimedia.org', 'image.api.playstation.com', 'images.stopgame.ru'],
    },
  };

export default nextConfig;

